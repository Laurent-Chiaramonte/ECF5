@model IEnumerable<MaintInfoBo.Tarif>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.date_tarif)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.montant_tarif)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.date_tarif)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.montant_tarif)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.tarifID }) |
            @Html.ActionLink("Details", "Details", new { id=item.tarifID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.tarifID })
        </td>
    </tr>
}

</table>
